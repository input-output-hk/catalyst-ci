VERSION 0.8

IMPORT ../../earthly/python AS python-ci
IMPORT ../../earthly/container AS container

builder:
    FROM container+main-builder --tool=python
    
    COPY --dir poetry.lock pyproject.toml Readme.md src .

check:
    FROM +builder

    DO python-ci+CHECK
